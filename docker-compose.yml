version: '3.8'

services:
  hermes-agent:
    build: .
    container_name: hermes-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - hermes_data:/root/.hermes

volumes:
  hermes_data:
    driver: local
